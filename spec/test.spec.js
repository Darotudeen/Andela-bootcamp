describe ('NotesApplication', function(){

	it('should add notecontent to noteslist', function(){
		var note_content = "";
		var Book = new NotesApplication.prototype();
		expect(Book.create(typeof "note_content").toBeDefined();
	});

	it('should arrange each notes in noteslist', function(){
		var Note_ID = this.notelist[i];
		var Book = new NotesApplication.prototype();
		expect(Book.noteslist.length).toBe(0);
		expect(Book.listNotes()).toBeDefined()
	
	});


	it('should return note_content as string', function(){
		var Note_ID = this.notelist[i];
		Book = new NotesApplication.prototype();
		expect(Book.get(Note_ID)).toBeDefined;
	});
	
	it('should return notes with a search string', function(){
		expect (str.search("text")).toBeDefined
	});
	
	
});

	
	
	
	
